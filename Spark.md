Делает параллельную обработку данных на кластере.
- Способ параллельного программирования

DWH (Data Warehouse или по русски Хранилище данных)
ETL (аббревиатура от extract, transform, and load или извлечение, преобразование и загрузка()

Делит большой файл на кусочки (партиции, пр. если ds = 2gb: `2*1024 / 128 = 16 зазделов`), которые обрабатываются на разных серверах в executer'ax - RDD (Resilient Distributed Dataset)

Shuffle — операция перемещения данных по узлам в результате выполнения операций соединения или агрегации Apache Spark.

Driver - управляет executer'ами

Hadoop имеет менеджер ресурсов YARN

Когда запускаем спарк, он обращается к hadoop через yarn и говорит, что нужно сделать



